/// Utility functions to work with 
/// Vector objects, which are plain objects 
/// that have x,y, and/or z number attributes.
singleton Topaz.Vector 



/// Returns the distance of this vector to the other vector.
///
staticfn Topaz.Vector.getDistance -> Number
    /// The first positional vector.
    @Object from 
    /// The second positional vector.
    @Object to

/// Sets the vector to a magnitude of 1. 
staticfn Topaz.Vector.normalize -> 
    /// The vector to modify.
    @Object value

/// Returns the cross product: A x B
///
staticfn Topaz.Vector.cross -> Object
    /// Vector B
    @Object a

    /// Vector B
    @Object b

/// Returns the dot product: A . B
///
staticfn Topaz.Vector.dot -> Object
    /// Vector B
    @Object a

    /// Vector B
    @Object b

/// Sets the X Y Z to integer values.
///
staticfn Topaz.Vector.floor -> 
    /// The vector to modify.
    @Object value;


/// Returns the YZ angle between the 2
/// vectors in relation to the origin in degrees
staticfn Topaz.Vector.rotationXDiff -> Number 
    /// The positional vector to measure from.
    @Object from

    /// The positional vector to measure to.
    @Object to


/// Returns the angle between the 2
/// vectors in relation to this in degrees
staticfn Topaz.Vector.rotationXDiffRelative -> Number 
    /// The positional vector to measure from.
    @Object from

    /// The positional vector to measure to.
    @Object to

/// Returns the angle of the vector in degrees about the X axis
///
/// If a vector has length zero in these dimensions
/// , the angle is always zero.
/// The rotation is about the directional vector (1, 0, 0) and
/// Positive rotation is towards the positive Z direction.
///
staticfn Topaz.Vector.rotationX -> Number 
    /// The positional vector to measure.
    @Object of

/// Returns the zx angle between the 2
/// vectors in relation to the origin in degrees
staticfn Topaz.Vector.rotationYDiff -> Number 
    /// The positional vector to measure from.
    @Object from

    /// The positional vector to measure to.
    @Object to

/// Returns the angle between the 2
/// vectors in relation to this in degrees
staticfn Topaz.Vector.rotationYDiffRelative -> Number 
    /// The positional vector to measure from.
    @Object from

    /// The positional vector to measure to.
    @Object to


/// Returns the angle of the vector about the Y axis in degrees
///
/// If a vector has length zero in these dimensions
/// , the angle is always zero.
/// The rotation is about the directional vector (0, 1, 0)
/// Positive rotation is towards the positive X direction.
staticfn Topaz.Vector.rotationY -> Number 
    /// The positional vector to measure.
    @Object of



/// Returns the XY angle between the 2
/// vectors in relation to the origin in degrees
staticfn Topaz.Vector.rotationZDiff -> Number 
    /// The positional vector to measure from.
    @Object from

    /// The positional vector to measure to.
    @Object to

/// Returns the angle between the 2
/// vectors in relation to this in degrees
staticfn Topaz.Vector.rotationZDiffRelative -> Number 
    /// The positional vector to measure from.
    @Object from

    /// The positional vector to measure to.
    @Object to

/// Returns the angle of the vector about the Z axis in degrees
/// These deals with angles between the x and y axes.
/// XY is most useful for 2D angles and transformations.
/// If a vector has length zero in these dimensions
/// , the angle is always zero.
/// The rotation is about the directional vector (0, 0, 1).
/// Positive rotation is towards the positive Y direction.
///
staticfn Topaz.Vector.rotationZ -> Number 
    /// The positional vector to measure.
    @Object of



/// Returns the result of rotating the vector
/// by the specified degrees about the center
staticfn Topaz.Vector.rotateX -> 
    /// The vector to rotate.
    @Number vector

    /// The amount to rotate in degrees.
    @Number amt

/// Returns the result of rotating the vector
/// by the specified degrees about the center
staticfn Topaz.Vector.rotateY -> 
    /// The vector to rotate.
    @Number vector

    /// The amount to rotate in degrees.
    @Number amt

/// Returns the result of rotating the vector
/// by the specified degrees about the center
staticfn Topaz.Vector.rotateZ ->
    /// The vector to rotate.
    @Number vector

    /// The amount to rotate in degrees. 
    @Number amt




/// Returns the length of the vector.
/// Write-only.
staticfn Topaz.Vector.length -> Number 
    @Object of


/// Adds 2 vector objects.
staticfn Topaz.Vector.add -> Object 
    /// The first operand
    @Object a 
    /// The second operand
    @Object b


/// Subtracts 2 vector objects.
staticfn Topaz.Vector.subtract -> Object 
    /// The first operand
    @Object a 
    /// The second operand
    @Object b

/// Multiplies 2 vector objects.
staticfn Topaz.Vector.multiply -> Object 
    /// The first operand
    @Object a 
    /// The second operand
    @Object b

/// Divides 2 vector objects.
staticfn Topaz.Vector.divide -> Object 
    /// The first operand
    @Object a 
    /// The second operand
    @Object b
